apiVersion: v1
kind: ConfigMap
metadata:
  name: sofa-config
data:
  auth_enabled: "true"
  # Test-specific configuration - uses the prefixed service names
  auth_issuer: "http://test-dex-service:5556/dex"
  auth_jwks_url: "http://test-dex-service:5556/dex/keys"
