apiVersion: v1
kind: ConfigMap
metadata:
  name: sofa-config
data:
  auth_enabled: "true"
  # Dev-specific configuration - uses the prefixed service names
  auth_issuer: "http://dev-dex-service:5556/dex"
  auth_jwks_url: "http://dev-dex-service:5556/dex/keys"
